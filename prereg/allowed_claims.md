# Allowed claims (from spec sections 25-26, condensed)
- V_b is maximum finite-state future b-regret extractable from the certified state.
- A structural atom exactly matches canonical debt changes on the reported finite specimen family.
- Under a finite kernel definition, two states are indistinguishable but require different values or successors.
- In the exact augmented finite model, same-(A,B) states share V_b^R; U_b^R spread is a history-cost observation.
- No counterexample was found in the frozen fresh bank.
- Exact counterexample identifies a missing distinction in a candidate representation.
- Only an explicit infinite actual-cost family with unbounded ratio supports disproof.
