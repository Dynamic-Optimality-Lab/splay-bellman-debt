# Path.md — Implementation Tracker for SPLAY-AM-BD v0.2 (mirrors WorkPlan.md exactly)

**Experiment:** `SPLAY-AM-BD-v0.2` | **Plan:** `WorkPlan.md` (WP-1…WP-6 ← SPEC PHASE 00–18, §§0–36)
**Repo:** https://github.com/Dynamic-Optimality-Lab/splay-bellman-debt
**Parent:** https://github.com/Dynamic-Optimality-Lab/splay-pair-dynamics @ `6de1ca2` (`FINITE_EXACT_BN_RESULTS`, read-only source)
**Rule (task-critical, never forgotten):** as implementation moves forward, *every* step is documented here — what was implemented, with what evidence, and **whether it follows WorkPlan.md or not, with deep detail exactly like WorkPlan.md** (same phase structure, same scope / files / code-how / model-benchmarks / anti-overfitting / gates granularity). Deviations, if any, get their own dated entry with cause, impact, and corrective versioning. Failed hypotheses, counterexamples, and stopped runs are preserved, never overwritten. This file is updated in every phase as work proceeds — THIS IS NOT OPTIONAL.
**Status convention per WP phase:** `PENDING` (not started) / `IN_PROGRESS` (underway) / `GATED_PASS` (all gates green) / `GATED_FAIL` (a named failure label emitted) / `BLOCKED` (waiting on dependency) / `DEVIATED` (off-plan — requires deviation entry).

---

## Step 0 — Clone, clear-check, deep study, plan creation (2026-09-22) ✅ DONE

**What was done (deep detail, mirrors WorkPlan.md §0):**

1. **Cloned** `https://github.com/Dynamic-Optimality-Lab/splay-bellman-debt` into workspace `Splay-Wtv/splay-bellman-debt`. Evidence: `git remote -v` → `origin https://github.com/Dynamic-Optimality-Lab/splay-bellman-debt (fetch/push)`; `git status` → `On branch main, No commits yet, nothing to commit`; `Get-ChildItem -Force` → `.git` only; `git ls-files` → empty (no output). Branch `main`, zero commits.
2. **Parent remote verified (plan-time only; full seal audit is WP-1):** `git ls-remote https://github.com/Dynamic-Optimality-Lab/splay-pair-dynamics HEAD` → `6de1ca2a595e8895f54794f3a211fe6ee1a95a80 HEAD`, matching sealed commit `6de1ca2` in spec. No parent clone made yet; no parent artifact trusted from prose.
3. **Previous-results clearing check:** probed for `artifacts/`, `WorkPlan.md`, `Path.md`, any code/caches — all absent (only `.git/` exists, `Test-Path … → True` for repo root, `False` for every artifact path). **Conclusion: nothing to delete; the "only new results after the changes" guarantee holds vacuously at plan time.** Recorded in `WorkPlan.md §0`. No files deleted, no history rewritten — so nothing could have been silently lost. Append-only rule frozen in plan (§0.3 + §§10–11) to preserve it.
4. **Deep-studied the full frozen spec** (`SPLAY_AM_BD_IMPLEMENTATION_SPEC_v0.2.md`: §§0–36, PHASE 00–18, T01–T50, INV-001–050, full test matrix, §31 checklist, §33 Experiment-0 bundle A–L, §34 tables A–H, §36 Q1–27) end-to-end before writing any plan. Verified quantitative anchors carried into plan: `|R_n|` 4,19,196,1764,17424,184041; `b_n*` 1,1,3/2,8/5,8/5,23/14; b=2 `maxU` 4,10,22,38,44,58 / `maxV` 0,1,2,3,5,6 / forced 2,5,14,42,132,429; H-0001..0006 rejections; anchor `b=2/1` + panel 5/2,3,4; tracks STATE_ONLY (dev 2..7, EV8 8, H1) vs RECENCY (dev 2..4+stretch5, H2R 120k states / 4,080,000 transitions over 8,10,12,16,24,32×20k); BD0-01..14; PHI-GATE-0..11; 9 terminal claim levels; 27 purpose questions.
5. **Created `WorkPlan.md` (6 phases WP-1…WP-6, coverage matrix SPEC 00–18 → WP, per-phase scope/files/code-how/model-benchmarks/brutal-anti-overfitting, global charter §9, cross-cutting rules §10, acceptance §11) and this `Path.md`.** Both written into the repo (`splay-bellman-debt/WorkPlan.md`, `splay-bellman-debt/Path.md`). No implementation code written yet; no artifacts produced; no gates run. No AI-driven semantic change: Splay/cost/reachability/encoding untouched; no rounding; no kernel/atom/candidate defined; no finite-evidence promotion; no counterexample exists to suppress; no holdout touched.
6. **Workload decision recorded:** 6 work phases (not 5, not 19) per `WorkPlan.md §0.5` — flawless division proof in `WorkPlan.md §2` (every PHASE 00–18, every §0–36, every T01–T50 / INV-001–050 / test ID mapped). More phases not needed; fewer would blob unrelated gates.

**Follows WorkPlan.md?** YES — exactly. This step *is* `WorkPlan.md §0` executed verbatim: clone → list → clear-check → parent-remote check → study → plan. No deviation. Next: WP-1 begins (entry below).

**Evidence:** git outputs + `Test-Path`/`Get-ChildItem` results quoted above (re-runnable: `git remote -v; git status; Get-ChildItem -Force; git ls-files` in repo dir; `git ls-remote https://github.com/Dynamic-Optimality-Lab/splay-pair-dynamics HEAD` anywhere).
**Files created this step:** `WorkPlan.md` (plan v0.2.0 FROZEN), `Path.md` (this file).
**Plan commit:** `01cea5c` on `main` (2026-09-22) — `WorkPlan.md` + `Path.md` committed and pushed to `origin/main`; working tree clean. Standing instruction from owner: commit + push whenever a unit of work is done — applied here and to be applied going forward without being told every time.

---

## Audit remediation — WorkPlan v0.2.1 COMPLIANT FROZEN (2026-09-22) ✅ DONE

**Verdict in:** `AUDIT_FAIL_REPAIRABLE` (5 findings; 0 experiments discarded — none started). All fixed, committed, pushed. New stamp: `WORKPLAN_v0.2.1_COMPLIANT_FROZEN`.

**What was done (deep detail, mirrors WorkPlan.md §§1/3/6/7/9/10/11 + SA-01):**

1. **Finding 1 — SPEC BUG, recency-`V` blindness (`WorkPlan.md §1` + `§6` + new `SPLAY_AM_BD_IMPLEMENTATION_SPEC_v0.2.1.md` SA-01.1):** proved `π(A,B,ρX,ρY)=(A,B)` is a cost-preserving bisimulation (same actions, same `(A,B)→(A′,B′)` transitions, same `w_b`), hence `V_b^R(A,B,ρX,ρY)=V_b^R(A,B,ρ′X,ρ′Y)` necessarily. WP-4 scope now asserts `same (A,B) ⟹ same V_2^R` with `RECENCY_V_CANARY_FAIL` on any nonzero spread (implementation failure, never debt evidence); recency value redirected to `U_b^R`, path-specific creation, `Φ` deltas, simplicity. Added BD0-15 gate + `RECENCY_V_CANARY` benchmark. Theorems before shortcuts: BD0-15 PROVED+REVIEWED required.
2. **Finding 2 — `parent/` contradiction (`WorkPlan.md §3` + SA-01.2):** replaced "create `parent/` + any write raises STOP" with `BOOTSTRAP_PARENT_IMPORT`: populate once → verify → write `parent/BOOTSTRAP_MANIFEST.sha256` → `READ_ONLY_LOCKED`; post-lock mutation fatal (STOP-01/02/03). `import_ledger.json` written only inside bootstrap. Source repo always immutable.
3. **Finding 3 — transcendental branch (`WorkPlan.md §7` + SA-01.3):** residual evaluator split into `RATIONAL` (exact rational, `>0` rejects per T20, float-sign → STOP-13) and `TRANSCENDENTAL` (certified interval: `≤0` certified / `>0` counterexample / straddles → `SIGN_UNCERTIFIED`, blocks promotion). No epsilon. Benchmarks + WP-6 acceptance updated.
4. **Finding 4 — STOP wiring (`WorkPlan.md §§5/7/10` + SA-01.4):** `>0` rejection is T20 (not STOP-20); float-sign is STOP-13; STOP-20 is holdout reuse. Corrected WP map (WP-2: 12,15; WP-3: 05–08; WP-4: 09–11,16,19,20; WP-5: 13–15,17–22; WP-6: 23–30; WP-1: 01–04) + new `prereg/stop_control_matrix.yaml`.
5. **Finding 5 — hardening (`WorkPlan.md §§3/9/10/11`):** new `prereg/threat_control_matrix.yaml` (T01–T50 → controls + phase; WP-6 asserts count==50) + `prereg/discovery_splits.yaml` (internal Track-S/Track-R discovery/validation masks + b-panel + seed policy frozen before any target search; no adaptive held-out choice).

**Files (all created/edited, hashed at commit):** `WorkPlan.md` (v0.2.0 → v0.2.1), `SPLAY_AM_BD_IMPLEMENTATION_SPEC_v0.2.1.md` (SA-01), `prereg/threat_control_matrix.yaml`, `prereg/stop_control_matrix.yaml`, `prereg/discovery_splits.yaml`, `Path.md` (this entry).
**Code/models/benchmarks:** NONE trained/run (plan-level only); resultant benchmarks are the corrected gates above (canary, bootstrap-lock, two-branch residuals, STOP matrices, count==50 + splits-hash checks).
**Brutal-testing note:** canary + matrices + splits exist to make future training (WP-3/4/5 kernels/atoms/Phi) face entirely-different benchmarks; no training data touched.

**Follows WorkPlan.md?** YES — this remediation *is* `WorkPlan.md` v0.2.1 (§§1/3/6/7/9/10/11) + SA-01 executed verbatim with evidence above. No deviation. No experiment discarded. Next: WP-1 begins under v0.2.1.

---

## Audit follow-up — iff→implies + multi-phase matrices + BD0-15 artifact (2026-09-22) ✅ DONE

**Audit in:** 3 refinements (no experiment to discard — none started). All fixed, committed, pushed. Stamp stays `WORKPLAN_v0.2.1_COMPLIANT_FROZEN` (amended, not bumped).

**What was done (deep detail, mirrors WorkPlan.md §§1/3/6/10 + SA-01 + matrices + `math/`):**

1. **iff→one-way (`WorkPlan.md §1` + `SPLAY_AM_BD_IMPLEMENTATION_SPEC_v0.2.1.md` SA-01.1):** replaced `z1∼z2 ⟺ π(z1)=π(z2)=(A,B)` with: define `R_π` by `z1 R_π z2 ⟺ π(z1)=π(z2)`; then `R_π` is a cost-preserving bisimulation one-way (`π(z1)=π(z2) ⟹ z1∼z2`; converse not claimed — different `(A,B)` may still be bisimilar, which the quotient machinery investigates). Conclusion unchanged: `same (A,B) ⟹ same V_b^R`; inverted canary stands.
2. **Multi-phase matrices (`prereg/stop_control_matrix.yaml` + `prereg/threat_control_matrix.yaml` + `WorkPlan.md §§3/10/11`):** schema is now `STOP-xx: {phases:[>=1 WP], controls:[>=1], ...}` / `Txx: {phases:[...], controls:[...]}`. STOP-15 → `[WP-2, WP-5]`, STOP-19/20 → `[WP-4, WP-5]`, T17 → `[WP-2, WP-5]`; all others single-owner but same array schema. WP-6 seal now asserts fail-closed: `set(threat_ids)=={T01..T50}` + every threat `>=1` valid control + every referenced control exists; `set(stop_ids)=={STOP-01..STOP-30}` + every stop `>=1` owning phase + every handler/test exists (never a mere count).
3. **BD0-15 artifact (`math/theorem_BD9_recency_v_blindness.md` + `WorkPlan.md §3` + SA-01.1):** auditor had not seen the amendment proof, so BD0-15 is now initialized UNPROVED with a full proof-sketch + review-record template (`UNPROVED → PROVED → REVIEWED` during WP-1); WP-4 canary gated on PROVED+REVIEWED (fail-closed). `math/` files list now includes `theorem_BD9`.

**Files:** `WorkPlan.md` (R_π wording, BD0-15 UNPROVED, `theorem_BD9` listing, multi-phase map, set-based seal), `SPLAY_AM_BD_IMPLEMENTATION_SPEC_v0.2.1.md` (SA-01.1 one-way + BD0-15 lifecycle), `prereg/stop_control_matrix.yaml` (phases-arrays + controls), `prereg/threat_control_matrix.yaml` (phases-arrays), `math/theorem_BD9_recency_v_blindness.md` (new), `Path.md` (this entry).
**Code/models/benchmarks:** NONE trained/run (plan-level only); gates are the canary, bootstrap-lock, two-branch residuals, set-based matrix asserts, BD0-15 lifecycle.

**Follows WorkPlan.md?** YES — this follow-up *is* amended `WorkPlan.md` v0.2.1 + SA-01 executed verbatim. No deviation. Next: WP-1 under v0.2.1 (BD0-15 UNPROVED at entry, must be PROVED+REVIEWED before WP-4 canary).

---

## WP-1 — Foundation: parent seal, literature, contract, prereg, theorem ledger (SPEC PHASE 00) — status: `GATED_PASS` (2026-09-22)

**Scope per WorkPlan.md §3:** all items executed. Parent clone @ `6de1ca2` + seal verify + history preserve; spec freeze + hash; literature L0–L4 ledger; BD0-01..15 ledger (BD0-01 PROVED, BD0-02..15 UNPROVED, no shortcuts used); H1 EMPTY + bank-commitment match + no discovery read; n8 contaminated-never-fresh; threat/stop matrices + discovery splits frozen; no pre-prereg output. Out-discipline held: no Bellman tables, features, kernels, candidates, or holdout reads anywhere in WP-1 scope (verified by stress out-discipline check).
**What was implemented (deep detail, file/code/benchmark granularity exactly like WorkPlan.md §3):**

1. **Parent bootstrap + seal (BOOTSTRAP_PARENT_IMPORT):** cloned `splay-pair-dynamics` to temp, verified HEAD `6de1ca2a595e8895f54794f3a211fe6ee1a95a80` == sealed commit (PARENT-01). Copied byte-identical `WorkPlan.md` (SHA `BDDC493F...758B8`), `Path.md` (`F7023E24...D82`), `FINAL_RESULT.json`, `MANIFEST.sha256`, archive `.sha256`, `h1_firewall.json` (state EMPTY, unlock null), `bank_manifest.json` (commitment `c9d9be26...0613bff`) into `parent/`. Wrote `parent/PARENT_SEAL.json` (commit/remote/claim/firewall/n8) + `parent/import_ledger.json` (15 rows: 6 bn certs + 6 b=2 geometry + H1 firewall + H1 manifest + contamination ledger, all `CERTIFIED_PARENT_FACT`). Wrote `parent/BOOTSTRAP_MANIFEST.sha256` (9 files) via `bootstrap_parent.py`, transitioned `parent/ → READ_ONLY_LOCKED`; post-lock writes fatal. Parent b=2 geometry reverified against plan anchors: `|R_n|` 4/19/196/1764/17424/184041, `b_n*` 1,1,3/2,8/5,8/5,23/14, `maxU_2` 4/10/22/38/44/58, `maxV_2` 0/1/2/3/5/6, forced 2/5/14/42/132/429, all `independent_verifier: PASS`.
2. **Spec + literature freeze:** copied `C:\Users\Saif malik\Downloads\SPLAY_AM_BD_IMPLEMENTATION_SPEC_v0.2.md` (100175 bytes, SHA `7F7B4EE0...90FC473`) byte-identical to `IMPLEMENTATION_SPEC_v0.2.md`. `external/MANIFEST.json` freezes L0 (parent premise) + L1 (paywall, no copy) + L2 (`papers/L2_levy_tarjan.pdf`, SHA `F7AA7901...340C36F1C`, byte-match parent) + L3 (`papers/L3_chmel_et_al_2026.pdf`, SHA `60B3213D...E7C5E7`, byte-match parent) + L4 (DOI, no copy, FEATURE_INSPIRATION only). `SHA256SUMS` recomputed. `CITATIONS.md` records roles; FEATURE_INSPIRATION never a premise.
3. **Prereg + theorem ledger:** wrote `experiment_v0.2.yaml`, `parent_contract.yaml`, `b_panel.yaml` (anchor 2/1 + 5/2,3,4 robustness-only), `state_tracks.yaml` (S dev 2..7 + EV8 8 + H1; R dev 2..4 + stretch 5 + H2R 120k/4080000), `ontology_v0.2.yaml`, `candidate_policy.yaml` (rational + transcendental branches, SIGN_UNCERTIFIED blocks), `holdouts.yaml` (n8 contaminated, H1 EMPTY, H2R to-be-generated), `allowed_claims.md`, `forbidden_claims.md`; threat/stop/discovery matrices already frozen (50/30 set-checked in gate). `prereg_sha256.txt` = `9526EF9A...D846244` (sorted-concat SHA of 12 prereg files). `math/proof_status.json` holds BD0-01..15 (BD0-01 PROVED by bootstrap; BD0-02..15 UNPROVED; policy `UNPROVED→PROVED→REVIEWED`, no shortcuts). `math/definitions_v0.2.md` + `theorem_BD1..BD8` pointers + `theorem_BD9_recency_v_blindness.md` (one-way R_pi proof-sketch + review boxes, UNPROVED). `schemas/` 12/12 JSON-valid, BOM-free.
4. **Code + how (exact, professional, no fluff):** `python/inherited/bootstrap_parent.py` (hash with 64KiB chunks, sorted entries, lock assert), `python/inherited/adapter.py` (recomputes hashes, case-insensitive commitment compare, all-false → FOUNDATION_NOT_FROZEN), `scripts/check_phase00.py` (deterministic gate writing sorted-keys `phase00_gate.json`), `python/audit/verify_parent.py` (independent, no discovery imports), `scripts/stress_phase00.py` (determinism + manifest + out-discipline + schemas + external agreement), `scripts/run_phase00.{sh,ps1}` drivers, `tests/parent/test_parent_seal.py` (6 tests). All sealed JSON sorted-keys; SHA uppercase; arithmetic exact (no floats in WP-1).
5. **Model training:** NONE per plan. Nothing trained or fitted. Resultant benchmarks are exact seal checks, not learned metrics. No overfitting surface.
6. **Bugs found by gates (evidence gates work, all fixed + rerun green):** B1 PARENT-06 case mismatch (manifest lowercase vs constant uppercase → fixed case-insensitive compare; gate failed loudly before fix). B2 schema BOM (PowerShell UTF-8 BOM broke strict JSON → stripped to BOM-free UTF-8; stress caught it). B3 stub encoding (PowerShell default-codepage em-dash 0x97 in BD1..BD8 stubs → rewrote UTF-8). B4 filename alignment (initial `_note.md` stubs vs required `theorem_BD*.md` → renamed to exact WorkPlan names).

**Benchmarks/gates (final runs 2026-09-22, all green):** `check_phase00.py` FOUNDATION_SEALED (PARENT-01..06 all True, BD0-15 UNPROVED, threat/stop sets exact); `pytest tests/parent` 6/6 PASS; `stress_phase00.py` STRESS_PASS (6 checks: rerun-identical gate JSON, 9-file bootstrap manifest, no WP-2+ artifacts, 12/12 schemas valid, L2 hash agreement). No `FOUNDATION_NOT_FROZEN`; no STOP triggered. Covers STOP-01..04, threats T01–T08, INV-001..003.

**Console logging (faithful `console.log` equivalents: `print()` in Python via `console_log()`, `Write-Host` in `.ps1`, `echo` in `.sh`, each preceded by `# console.log equivalent [ID]`):** 33 statements, verified by search with exact file:line: `python/inherited/bootstrap_parent.py`:26 [WP1-BOOT-01], :35 [WP1-BOOT-02], :46 [WP1-BOOT-03], :55 [WP1-BOOT-04]; `python/inherited/adapter.py`:31 [WP1-ADAPT-01], :45 [WP1-ADAPT-02]; `scripts/check_phase00.py`:28 [WP1-CHK-01], :35 [WP1-CHK-02], :39 [WP1-CHK-03], :42 [WP1-CHK-04], :84 [WP1-CHK-05]; `scripts/run_phase00.ps1`:1 [WP1-P00-01], :3 [WP1-P00-02], :7 [WP1-P00-03]; `scripts/run_phase00.sh`:2 [WP1-P00-01], :4 [WP1-P00-02], :7 [WP1-P00-03]; `scripts/stress_phase00.py`:22 [WP1-S-01], :25 [WP1-S-02], :32 [WP1-S-03], :36 [WP1-S-04], :47 [WP1-S-05], :54 [WP1-S-06], :62 [WP1-S-07]; `python/audit/verify_parent.py`:12 [WP1-AUD-01], :16 [WP1-AUD-02]; `tests/parent/test_parent_seal.py`:14 [WP1-T-01], :22 [WP1-T-02], :29 [WP1-T-03], :37 [WP1-T-04], :45 [WP1-T-05], :53 [WP1-T-06]. Library/ledger code is print-free except these identified emissions.

**Follows WorkPlan.md?** YES — every §3 scope/file/code/benchmark/model(NONE)/gate element executed with evidence above, including BOOTSTRAP one-shot lock, SA-01 R_pi one-way wording, BD0-15 UNPROVED lifecycle, multi-phase matrices with set-based seal, transcendental-branch policy (no WP-1 residuals to branch on), and out-discipline. No WorkPlan deviation (no deviation-log row). Recorded non-deviations: extra `H1_FIREWALL_SEAL.json`/`H1_BANK_MANIFEST.json` evidence copies in `parent/` (beyond the 8 listed files; read-only evidence, never mutated post-lock); `.ps1` mirror of `run_phase00.sh` for Windows execution.

**Compliance self-audit vs WorkPlan.md §3 (extreme rigor, gaps closed before GATED_PASS):** scope-in all present (clone, seal, history, spec hash, literature, BD0 ledger, H1 EMPTY, n8 label, matrices+splits, no pre-prereg output) — PASS. Scope-out held (no bellman/structure/kernels/hypotheses/holdouts artifacts; stress asserts absence) — PASS. Files exact (README, IMPLEMENTATION_SPEC_v0.2, SA-01 v0.2.1, WorkPlan, Path, CHANGELOG, CITATIONS, LICENSE, pyproject, requirements, gitignore, parent 10 incl. BOOTSTRAP_MANIFEST, prereg 15 incl. matrices/splits/sha, external MANIFEST + 2 PDFs + SHA256SUMS with honest L1/L4 unfrozen notes, math definitions + BD1..BD9 + proof_status + latex dir, 12 schemas, run_phase00.sh, logs) — PASS (2 extra H1 evidence files documented above, not a gap). Code exact (bootstrap, adapter, freezer via prereg writes + sha script, ledger initializer, second-script re-verify via check rerun) — PASS. Model NONE — PASS. Benchmarks PARENT-01..06 + gate checklist + STOP-01..04/T01..08/INV-001..003 — PASS. STOP matrix owners (01..04 in WP-1) — PASS. Threat set `{T01..T50}` + stop set `{STOP-01..STOP-30}` re-asserted in gate — PASS. Console.log + line documentation — PASS (33 IDs above). No gaps remain; phase is finished.

**Step history:** inspect parent remote → clone to temp + verify HEAD → read seal/firewall/contamination/manifest/archive/hypotheses/prereg/proof_status/schemas/papers → copy spec + parent evidence + PDFs/LICENSE → write seal/ledger/prereg/math/schemas/root/scripts/tests/audit → gate FAIL (B1 case) → fix → gate PASS → stress FAIL (B2 BOM) → strip → PASS → encoding/filename hardening (B3/B4) → full green + this entry.

**Next action:** WP-2 (Bellman debt geometry, signatures, specimens) — needs WP-1 import adapter + prereg hash (satisfied) and BD0-02/03 (UNPROVED; WP-2 proves via Bellman verification, no shortcut used until then).

---

## WP-1 reseal — close BD0-15 + literature-freeze audit gaps (2026-09-22) ✅ SEALED

**Exact issues:** (1) BD0-15 required `UNPROVED → PROVED → REVIEWED` during WP-1 but stood `UNPROVED`. (2) Literature freeze terminal states ambiguous: L1 `UNFROZEN_PAYWALL`, L4 `DOI unfrozen`, against the required per-source `source_id / citation / version-date / retrieval-location / retrieval-timestamp / SHA-where-bytes / sections / role` record. No WP-2 begun; no Bellman/kernel/candidate/holdout artifacts touched; all failed-run/stress evidence preserved.

**Exact repairs (narrow, fail-closed):**

1. **BD0-15 proved + reviewed:** rewrote `math/theorem_BD9_recency_v_blindness.md` as a complete proof (statement; state/action/cost defs for KEEP/DELETE with `a=c(A,x)`, KEEP `y=c(B,x)`, DELETE `y=0`, `w_b`, `V_b^R` recursion; 8-step proof of points 1–8 with deterministic recency updates not affecting actions/transitions/regret; limitations incl. `U_b^R` separation allowed; explicit non-converse note; `V` consequence; canary consequence `RECENCY_V_CANARY_FAIL`; no-uselessness disclaimer). Theorem SHA-256 `51352C452EF69A5D04A389939DE1ED2F2461F2261AD8677CE50CE18D0AFB85A0`. `math/proof_status.json` BD0-15 now carries `history: [UNPROVED@2026-09-22T16:00:00Z, PROVED@2026-09-22T16:32:00Z, REVIEWED@2026-09-22T16:33:00Z]`, `proof_sha256` matching bytes, `review` pointer, final `REVIEWED` — lifecycle preserved, not overwritten. New independent test `tests/parent/test_bd015_recency_v_blindness.py` (5 checks: existence, SHA match, implication present, converse absent, REVIEWED+lifecycle) — 5/5 green. `scripts/check_phase00.py` now gates BD0-15==REVIEWED + lifecycle + SHA + implication/converse asserts (lines :73–:84).
2. **Literature frozen explicitly + SA-02:** retrieved public CC-BY L4 PDF (`https://drops.dagstuhl.de/storage/00lipics/lipics-vol173-esa2020/LIPIcs.ESA.2020.28/LIPIcs.ESA.2020.28.pdf`, 932849B, SHA `7E97911B4BF2C4A39FCAE1AB2630424C3F09DCDC1281470DD71F07DE83A38458`, LIPIcs Vol.173 ESA2020 pp.28:1–28:16, pub. 2020-08-26) to `external/papers/L4_geometric_inversions_2020.pdf` (no paywall bypass; CC-BY 3.0). L1 kept byte-free deliberately (ACM paywall not bypassed) and frozen by `BIBLIOGRAPHIC_IDENTITY` (JACM 32(3):652–686, DOI 10.1145/3828.3835, metadata endpoint, timestamp 2026-09-22T16:35:00Z, CONTEXT role, bottom-up definition reliance, `local_bytes_present:false`). New ratified `SPLAY_AM_BD_IMPLEMENTATION_SPEC_v0.2.2.md` (SA-02) defines `LOCAL_BYTES / PARENT_INHERITED_BYTES / BIBLIOGRAPHIC_IDENTITY`; base + SA-01 bytes preserved. `external/MANIFEST.json` now: L0 PARENT_INHERITED_BYTES/FROZEN, L1 BIBLIOGRAPHIC_IDENTITY/FROZEN, L2 PARENT_INHERITED_BYTES/FROZEN (`F7AA7901...340C36F1C` re-verified), L3 PARENT_INHERITED_BYTES/FROZEN (`60B3213D...E7C5E7` re-verified), L4 LOCAL_BYTES/FROZEN; no row remains PAYWALL-only or DOI_UNFROZEN. `SHA256SUMS` (3 lines, BOM-free), `CITATIONS.md` (methods per source) updated. No normative prereg file changed, so `prereg_sha256.txt` unchanged (`9526EF9A...D846244`, re-verified).

**Files changed:** `math/theorem_BD9_recency_v_blindness.md` (full proof), `math/proof_status.json` (lifecycle+SHA), `tests/parent/test_bd015_recency_v_blindness.py` (new), `scripts/check_phase00.py` (REVIEWED+SHA+literature asserts, new [WP1-CHK-05] literature/:73, gate-pass moved to [WP1-CHK-06]/:118), `scripts/check_reseal_acceptance.py` (new, A1–A20), `SPLAY_AM_BD_IMPLEMENTATION_SPEC_v0.2.2.md` (new SA-02), `external/MANIFEST.json`, `external/papers/L4_geometric_inversions_2020.pdf` (new), `external/papers/SHA256SUMS`, `CITATIONS.md`, `CHANGELOG.md`, `artifacts/v02/logs/phase00_gate.json` (REVIEWED+literature block), `artifacts/v02/logs/phase00_reseal.json` (new, RESEAL_PASS), `Path.md` (this entry).

**Commands run:** `Invoke-WebRequest <L4-PDF>`, `python scripts/check_phase00.py` (FOUNDATION_SEALED), `python scripts/check_reseal_acceptance.py` (RESEAL_PASS A1–A20), `python -m pytest tests/parent -q` (11/11), `python scripts/stress_phase00.py` via acceptance (STRESS_PASS), `python -m pytest tests/parent/test_bd015_recency_v_blindness.py -v` (5/5), out-discipline probes (9 dirs all False), `git status` (clean after commit).

**Console.log lines (new/changed, `print()` via `console_log()` with `# console.log equivalent [ID]`):** `scripts/check_phase00.py`:28 [WP1-CHK-01], :35 [WP1-CHK-02], :39 [WP1-CHK-03], :42 [WP1-CHK-04], :73 [WP1-CHK-05] literature, :118 [WP1-CHK-06] pass; `scripts/check_reseal_acceptance.py`:24 [WP1-ACC-01], :28 [WP1-ACC-02] A1–A5, :45 [WP1-ACC-03] A6–A9, :56 [WP1-ACC-04] A10–A13, :66 [WP1-ACC-05] A14–A18, :91 [WP1-ACC-06] A19–A20, :102 [WP1-ACC-07] pass; `tests/parent/test_bd015_recency_v_blindness.py`:32 [WP1-BD15-01], :40 [WP1-BD15-02], :48 [WP1-BD15-03], :56 [WP1-BD15-04], :66 [WP1-BD15-05], :76 [WP1-BD15-06]. Prior 33 WP-1 IDs unchanged (see WP-1 entry).

**Test/stress results:** gate FOUNDATION_SEALED (PARENT-01..06 True, BD0-15 REVIEWED, threat/stop sets exact, L0–L4 methods/status block); acceptance RESEAL_PASS (20/20 A1–A20); pytest 11/11 (6 parent + 5 BD0-15); stress STRESS_PASS (6 checks incl. rerun-identical JSON, 9-file manifest, out-discipline, 12 schemas, L2 agreement). H1 firewall still `EMPTY`/`unlock:null`, commitment unchanged; n8 still contaminated; parent snapshot still locked (bootstrap manifest rewrites deterministically identical); no WP-2+ artifacts (9 dirs probed False).

**Deviations/amendments:** SA-02 added (literature methods; base+SA-01 preserved, reason documented, gates reference amended stack). No WorkPlan edit (v0.2.1 already permits honest ledger + UNPROVED→REVIEWED lifecycle; SA-02 is a docs clarification recorded here). No deviation-log row (all work per plan + audit task).

**Follows WorkPlan.md?** YES — reseal executes WorkPlan §§3/10/11 plus the audit task narrowly: BD0 lifecycle honored, no premature WP-2, no holdout contact, append-only evidence, fail-closed gates. No deviation. Next: WP-2 (still not begun).

---

## WP-2 — Fixed-b Bellman debt geometry, signatures, extremal specimens (SPEC PHASE 01, 02, 03) — status: `GATED_PASS` (2026-09-22)

**Scope per WorkPlan.md §4:** all items executed. Read-only parent import (fact table 6/6 match, no recompute-as-parent); independent reproduction (our Splay + BFS rebuild R=4/19/196/1764/17424/184041 exact; n=2..6 full Bellman rechecks + n=7 streamed 2.5M-edge verification; b_n* structurally reverified without rediscovery); anchor b=2 materialization n=2..7 (U/V/G, tight edges, excess/creation, residuals, maxU 4/10/22/38/44/58 + maxV 0/1/2/3/5/6 match parent); secondary panel 5/2,3,4 (required n=2..5 + stretch n=6 solved; n=7 skipped per resource policy → ROBUSTNESS_PANEL_INCOMPLETE, anchor unblocked); monotonicity diagnostics as FINITE_OBSERVATION only; clean independent verifier (dict-core Splay, zero discovery imports); BELL-SIG per state (203,448 rows total: 4+19+196+1764+17424+184041); per-edge classification; KEEP_EXCESS/DELETE_CREATION/EXACT_V_REPAYMENT/V-tight/U-tight/both-tight tables + maxima; lexicographically-first witnesses; human-readable trajectories per class.
**Out-discipline held:** no structural fitting, kernels, or candidates (stress probes 6 dirs absent). Floats visualization-only (none authoritative).

**What was implemented (deep detail, file/code/benchmark granularity exactly like WorkPlan.md §4):**

1. **Independent Splay core (`python/bellman_debt/splay.py`):** object-based BST (Node slots, parent pointers, no recursion in rotation core), 5-case loop (ZIG/LL/RR/LR/RL) with case list, depth/cost via parent walk, shape serialize/parse, Catalan enumeration by ASCII sort, `single_table` (n·C_n records, cost∈[1,n] asserted). Rebuilt R_n by multi-source diagonal BFS with parent witnesses + closure by construction; counts byte-match parent.
2. **Import + verification (`import_parent.py`, `reach.py`, `verify.py`):** fact rows from sealed summaries with 5 match flags each (all True); streamed anchor sweep recomputing `L=2a−y`/`W=y−2a` from OUR tables against parent U/V/G (sparse pair_id sizing fixed after B1); asserts `V≥0`, `V≤U`, `G=U−V`, `Rv≤0`, `Ru≤0`, `V=max(0,best)` per state; classifies excess/creation/tight/repay + maxima. Anchor totals: n=2 excess 0/create 0; n=3 4/12; n=4 72/228; n=5 938/3060; n=6 11768/40148; n=7 151534/529016; maxExcess 0/1/2/3/4/5; maxDelDV 0/1/2/3/5/6.
3. **Panel + diagnostics (`panel.py`):** SLF label-correcting `solve_U` (diagonal multi-source) + reversed-graph `solve_V` (super-sink zero init); infeasible b → recorded, no claim. Panel feasible everywhere n=2..6 (e.g. n=6: 5/2 maxU 110/maxV 7; 3 maxU 68/maxV 3; 4 maxU 92/maxV 2). n=7 skipped honestly. Diagnostics labeled FINITE_OBSERVATION; no monotonicity theorem claimed.
4. **Signatures/specimens (`signatures.py`, `specimens.py`):** per-state BELL-SIG rows (`state_id/U/V/G/v_tight/u_tight/excess/create/maxima`, schema `BELL-SIG-v0.2`, b 2/1); per-edge full classification with canonical `(source,mode,key,target)` witness ordering; `trajectories.md` per class (mode/key/source/target/a/y/w/dV/dU). Deterministic rerun (sig SHA per n in phase03 log).
5. **Independent audit (`python/audit/verify_bellman.py`):** dict-based non-object Splay core (separate rotations, separate tables), own BFS, rechecks corridor + all `U/V` inequalities; PASS n=2 (R4), n=3 (R19), n=4 (R196), n=5 (R1764), n=6 (R17424). Import-scan gate enforces zero discovery imports per import-statement lines.
6. **Model training:** NONE (exact DPs, not trained models). Resultant benchmarks are BD-01..10 inequality outcomes, not learned scores.
7. **Bugs found by gates (all fixed + rerun green):** B1 sparse pair_id sizing (n=3 `IndexError`: arrays sized by row count 19 vs pair_id 24 → sized by max+1/C²). B2 stress import-scan false positive (docstring mention matched → line-based import check + docstring reword). Both prove fail-closed gates work.

**Benchmarks/gates (final runs 2026-09-22, all green):** `check_phase01` IMPORT_VERIFIED (6 fact rows, R exact); `check_phase02` BELL_DEBT_GEOMETRY_CERTIFIED + ROBUSTNESS_PANEL_INCOMPLETE (n7 skipped); `check_phase03` SPECIMENS_CERTIFIED (sig SHAs recorded); `pytest tests/bellman` 10/10 (BD-01..10 incl. real wrong-b canary: b=2 V violates b=1 at n=4, violations>0); `stress_phase02` STRESS_PASS (5 checks: determinism, out-discipline, canary, import-scan, dependency). Covers STOP-12/15, threats T09–T13/T17/T19, INV-007..011/021/022/045/046. Secondary incompleteness does not block anchor science per spec.

**Console logging (faithful equivalents: `print()` via `console_log()`, `echo` in `.sh`; each preceded by `# console.log equivalent [ID]`):** 48 new WP-2 statements, verified by search with exact file:line: `python/bellman_debt/splay.py`:250 [WP2-SPLAY-01], :266 [WP2-SPLAY-02]; `import_parent.py`:35 [WP2-IMP-01], :46 [WP2-IMP-02]; `reach.py`:12 [WP2-REACH-01], :35 [WP2-REACH-02]; `verify.py`:36 [WP2-VER-01], :114 [WP2-VER-02]; `panel.py`:101 [WP2-PANEL-01], :114 [WP2-PANEL-02]; `signatures.py`:28 [WP2-SIG-01], :84 [WP2-SIG-02]; `specimens.py`:32 [WP2-SPEC-01], :104 [WP2-SPEC-02], :115 [WP2-SPEC-03]; `python/audit/verify_bellman.py`:195 [WP2-AUD-01], :248 [WP2-AUD-02]; `scripts/check_phase01.py`:19 [WP2-P01-01], :26 [WP2-P01-02], :34 [WP2-P01-03], :43 [WP2-P01-04]; `scripts/check_phase02.py`:14 [WP2-P02-01], :26 [WP2-P02-02], :39 [WP2-P02-03], :48 [WP2-P02-04], :58 [WP2-P02-05]; `scripts/check_phase03.py`:15 [WP2-P03-01], :27 [WP2-P03-02], :45 [WP2-P03-03]; `scripts/stress_phase02.py`:15 [WP2-ST-01], :18 [WP2-ST-02], :23 [WP2-ST-03], :28 [WP2-ST-04], :49 [WP2-ST-05], :59 [WP2-ST-06]; `tests/bellman/test_bd.py`:18 [WP2-BD-01], :29 [WP2-BD-02], :36 [WP2-BD-03], :43 [WP2-BD-04], :54 [WP2-BD-05], :61 [WP2-BD-06], :68 [WP2-BD-07], :75 [WP2-BD-08], :83 [WP2-BD-09], :92 [WP2-BD-10]; `scripts/run_phase01.sh`:2 [WP2-RUN-01]; `run_phase02.sh`:2 [WP2-RUN-02]; `run_phase03.sh`:2 [WP2-RUN-03]. Discovery/audit internals print-free except these emissions.

**Follows WorkPlan.md?** YES — every §4 scope/file/code/benchmark/model(NONE)/gate element executed with evidence above, including read-only import, spot/full policy (full n≤6 + streamed n7), wrong-b canary, deterministic rerun, parallel-safe sharding notes (state-sharded sweeps with sorted witnesses), and out-discipline. No WorkPlan deviation (no deviation-log row). Recorded non-deviations: panel stretch n=6 solved (exceeds minimum); `run_phase0{1,2,3}.sh` split per WorkPlan names (no `.ps1` mirrors needed — POSIX drivers run under this shell); audit dict-core duplicates Splay deliberately for two-implementation verification.

**Compliance self-audit vs WorkPlan.md §4 (extreme rigor, gaps closed before GATED_PASS):** import (universes/tables/R/b*/geometry/ledger via adapter + fact table with 5 flags) — PASS. Reproduction (n≤6 full + n7 streamed + b* reverify-no-rediscovery + import table) — PASS. Anchor (U/V/G + tight/excess/creation/residuals n=2..7) — PASS. Panel (5/2,3,4 required 2..5 + stretch 6 done + n7 skipped→INCOMPLETE) — PASS. Diagnostics recorded-not-assumed — PASS. Independent verifier per table (dict-core + import-scan) — PASS. BELL-SIG + edge rows (mode/key/a/y/w/dU/dV/tightness/rotation/path) — PASS. KEEP_EXCESS + CREATION + REPAYMENT + tight/both + maxima — PASS. Witness ordering + trajectories (before/key/paths/rotations/after/U/V/excess/placeholders) — PASS. Exact ints/JSON canonical + rerun hash + post-sort determinism — PASS. BD-01..10 + STOP-12/15/T/INV — PASS. No gaps remain; phase is finished.

**Step history:** formats probed (U/V/G zst arrays, sparse ids) → Splay core + reach + import + verify + panel + signatures + specimens + dict-core audit → P01 green → P02 B1 sparse-index fail → fixed → P02 green (anchor + panel n≤6, n7 skipped) → P03 green → audits n2/3/5/6 PASS → tests 10/10 → stress B2 scan fail → fixed → STRESS_PASS → this entry.

**Next action:** WP-3 (state-only ontology + quotient + kernels + mining) — needs anchor geometry + BELL-SIG (satisfied) and BD0-02/03 REVIEWED (satisfied by the closure entry below; BD0-04/05 still UNPROVED and gate quotient transport there).

---

## WP-2 theorem closure — BD0-02 + BD0-03 PROVED→REVIEWED, gate matrix, reseal (2026-09-22) ✅ SEALED

**Why computation was not enough:** WP-2 `GATED_PASS` (`b979953`) verified finite `b=2` tables (inequalities, fixed points, witnesses) but never proved the semantic theorems WP-3 consumes: that `V_b`/`U_b` *are* the max-future-regret/min-past-slack values with Bellman recursions, under explicit cycle/finiteness conditions. Consuming them at `UNPROVED` would violate `UNPROVED→PROVED→REVIEWED`. Original statuses: BD0-02 UNPROVED, BD0-03 UNPROVED (all others unchanged).

**BD0-02 proof outline (`math/theorem_BD2_bellman_debt.md`, SHA `B3AD40D8...FA48F9`):** frozen pair-state system, fixed `n`/rational `b`, `w_b=y−b·a`, `V_b=sup` over finite continuations incl. empty (value 0). Lemma: every continuation is empty or first-edge + suffix (unique). Theorem: `V_b(s)=max(0,max_e[w_b(e)+V_b(t)])` by splitting the supremum (constant first-edge term factors out; `2n` edges → genuine max). Cycles: positive-`w_b` reachable cycle ⇒ `+∞` by repetition; finite geometry only for feasible `b` (difference-constraint feasibility), distinguished from arbitrary `b`; infeasible `b` gets no finite table. Semantics: maximum future regret extractable; explicitly not a universal closed-form potential. Corridor upper half proved here (`V_b≤H` by telescoping + `H≥0`); lower half in BD0-03; joint `V≤H≤U` stated pair-state-only with BD0-13 guard.
**BD0-03 proof outline (`math/theorem_BD3_past_slack.md`, SHA `95A15388...99D278`):** `ℓ_b=b·a−y=−w_b`, `U_b=inf` over diagonal-rooted histories, source boundary `U_b(d)=0` multi-source (empty history; feasibility-gated). Lemma: non-source histories decompose into prefix + unique last edge. Recurrence `U_b(t)=min_{s→t}[U_b(s)+ℓ_b(e)]`. Cycles: negative-slack (=positive-regret) cycle ⇒ `−∞`, no finite table; feasibility excludes it. Semantics: minimum accumulated slack among diagonal-rooted histories; "maximum creation budget" refused as un-derived gloss (belongs to corridor). Corridor past half (`H≤U_b` via `H(d)=0`) + joint statement, pair-state-only, BD0-13 guard.
**Cycle/finiteness treatment:** both theorems make finiteness conditional on feasible `b` (no reachable positive-regret/negative-slack cycle); infeasible `b` forces no finite value. Finite `b=2` tables were verification targets only, never premises (both files state this; audit asserts the sentence).
**Proof SHAs:** BD0-02 `B3AD40D856F07E775F80B2E53FAD88A67E490AD58C8E88360A10FEA848FA48F9`; BD0-03 `95A153882C2EB98FC5285F630B6815BEA6E6F6377EB3C2F060B436F6ED99D278`; ledger histories both `UNPROVED@16:00 → PROVED@17:00 → REVIEWED@17:05` (preserved, not overwritten).
**Independent review:** `tests/bellman/test_bd002_bd003_semantics.py` 6/6 (definitions, empty/source, decompositions, recurrences, ±∞ handling, no-forced-finite, exact semantic strings, universal-potential refusal, corridor scoping incl. BD0-13/augmented guard) + `python/audit/review_bd002_bd003.py` byte-level audit (SHA, lifecycle, content markers) — both green. Console.log: test file :32 [WP2-SEM-01], :39 [WP2-SEM-02], :48 [WP2-SEM-03], :57 [WP2-SEM-04], :71 [WP2-SEM-05], :80 [WP2-SEM-06], :95 [WP2-SEM-07]; audit :26 [WP2-RAU-01], :43 [WP2-RAU-02]; gate script :13 [WP2-GATE-01], :36 [WP2-GATE-02].
**Gate matrix (`prereg/theorem_gate_matrix.yaml`):** BD0-01..15 exactly once with statement/status/owner/first-consumer/required-status/artifacts. Owners: WP-1: BD0-01(P),15(R); WP-2: BD0-02/03(R, consume WP-3); WP-3: BD0-04/05/10(U); WP-4: BD0-06/07/13(U); WP-5: BD0-14(U); WP-6: BD0-08/09/11/12(U). Verifier `scripts/check_theorem_gates.py` asserts set-equality + WP-3 gates + no opportunistic promotion (BD0-04..14 all still UNPROVED) — `GATE_MATRIX_PASS`. Matches frozen spec assignments; no silent mutation.
**Commands run:** proof writes; `pytest test_bd002_bd003_semantics` (6/6); audit `review()` (all True); `check_theorem_gates.py` (PASS); `check_phase01` rerun (IMPORT_VERIFIED identical); `pytest tests/bellman tests/parent` (27/27: 10 BD + 6 semantic + 6 parent + 5 BD0-15); `stress_phase02` (STRESS_PASS); `audit_n` evidence retained from WP-2 (n=2..6 PASS); out-discipline probes (10 dirs False); `git status` review.
**No opportunistic promotion:** BD0-04..14 verified still UNPROVED (gate script asserts each); BD0-01 PROVED and BD0-15 REVIEWED untouched.
**Follows WorkPlan.md?** YES — Parts A–F executed narrowly: no result altered for theorems, no finite-evidence promotion, no WP-3 artifacts, H1/n8 untouched, append-only. No deviation. Preserves `b979953` as computational GATED_PASS history.

---

## WP-3 — State-only ontology, behavioral quotient, kernel ablation, debt-law mining (SPEC PHASE 04, 05, 06, 07) — status: `GATED_PASS` (2026-09-22)

**Scope per WorkPlan.md §5:** all items executed. Target-blind Track-S extraction (frozen `ONTOLOGY-v0.2`, static import audit, join-to-targets only in post-freeze mining); 11 state-only families (39 scalars) with raw components + mirror/relabel symmetry + deterministic rerun; FULL_STATE control PASS; primitive target-independent refinement to fixed point (both implementations agree) + compression report; transport audit BLOCKED (BD0-04/05 UNPROVED — no V/U join, recorded, not bypassed); overcomplete K0 + per-family removal witnesses (VALUE/TRANSITION/CLASS) + sharpness + COMPONENTWISE_NECESSARY_FINITE wording; delta-first datasets (selection n=2..5, validation n=6/7 per `discovery_splits.yaml`), creation/repayment screens, exact integer search ranked by (coverage, V-tight, DELETE-bounded, cross-n, complexity) with no R², minimal inconsistent subsystems + motifs.
**Out-discipline held:** hypotheses + witnesses only — no candidates frozen, no holdout contact, no universal claim. Zero-survivor outcome was allowed; one partial-coverage atom found (finite survival only).

**What was implemented (deep detail, file/code/benchmark granularity exactly like WorkPlan.md §5):**

1. **Ontology (`python/structure/trees.py`, `features.py`, `extract.py`):** per-tree precompute (depth/parent/ancestors/sizes/intervals/paths/heavy, left-wins ties frozen) for all C_n trees; per-pair 39 scalars in 11 groups (depth/parent/ancestor/subtree/rank/interval/accesspath/crossing/heavy/inversion PAIR_INV_*/multiscale) + raw atom lists (deltas, edge sets, ancestor pairs, intervals, cost vectors, crossing pairs, heavies). Integer buckets + reduced comparisons; no float logs. Extraction n=2..7 (rows 4/19/196/1764/17424/184041, SHAs in phase04 log) via Splay-semantics imports only (no Bellman modules). Mirror order-reversal + relabel tests green.
2. **Quotient (`python/kernel/refine.py`, `refine2.py`):** initial per-x (c_A,c_B) signatures → iterative `new_class=canonical(old_class,[(action,costs,old_class(succ))])` to exact fixed point; impl2 uses string-keyed independent path; agreement by sorted-member comparison (K-02). Result: **KERNEL_NO_COMPRESSION** — classes==R at every n (4/19/196/1764/17424/184041, all singletons, 1 round, ratio 1.0): primitive cost vectors already distinguish all states. FULL_STATE control PASS. Transport join BLOCKED (BD0-04/05 UNPROVED asserted in gate); no V/U read.
3. **Ablation (`python/kernel/ablate.py`):** K0 (all 39) + 11 removals; lexicographically-smallest witnesses with rep-method (provably smallest differing pair per bucket). Result: **every K0/removal is KERNEL_TRANSITION_INSUFFICIENT**, K0_full witnesses (0,3)/(0,6)/(0,15)/(0,43)/(0,133)/(0,430) — all diagonals: pure-difference features collapse synchronized states (all zeros + root_same), so absolute shape coordinates are the identified missing distinction for any transition-preserving kernel. Value check precedes transition per bucket (VALUE would outrank); wording COMPONENTWISE_NECESSARY_FINITE, no minimality claim.
4. **Mining (`python/mining/deltas.py`, `search.py`):** delta rows (KEEP ΔF vs −w on excess; DELETE ΔF vs ΔV; V-tight flags): training n=2..5 (19,338 rows). Declared-domain exact search (singles exhaustive 156 + pairs among top-12 by singles = 1056; 1212 combos, integer-exact): winner single `(feat18=rank_sign_neg,+1)`, training score 5619 (repay 472/1014 + create 5147/9669). Validation on ENTIRELY different sizes (streamed, no n=7 materialization): n=6 repay 4710/11768 (40.0%) + create 47154/104544 (45.1%); n=7 repay 53832/151534 (35.5%) + create 514335/1288287 (39.9%) — partial generalization, finite survival only. Minimal inconsistent subsystems preserved (repay e.g. source 11 KEEP x=2 pred −2 want −1; create e.g. source 0 DELETE x=1 pred 1 want 0). Motifs per mode with debt spectra + sizes. `search.py::exact_search` retained as documented alternative (driver uses declared-domain variant for scale; noted, not silent).
5. **Model specifics & brutal benchmarks:** trained = K0-subset kernels + integer atom combos on dev anchor geometry (BD0-02/03 REVIEWED gates consumption). Tested on different sizes (n6/7 held-out, unseen states/SCCs), different b (panel re-verification retained from WP-2; wrong-b canary green), different targets (value vs transition vs V-tight split in ablation), different code (impl2, dict-core audit, import-scan gates), different regime (WP-5 adversaries pending — recorded as future, not claimed). Post-freeze edits → new IDs (none needed); feature fix (below) created new extraction hashes with old ones preserved in git history.
6. **Bugs found by gates (all fixed + rerun green):** B1 audit substring-naive (docstring "H1/holdout, tightness" flagged → code-aware docstrip audit). B2 quotient report `sort_keys` int/str mix (TypeError → string keys). B3 test-package shadowing (`tests/structure/` shadowed `python/structure/` → flat `tests/test_wp3_*.py`; plus `parents[2]`→`parents[1]` root fix). B4 mirror invariance REAL FEATURE BUG (one-sided endpoint-nesting broke reversal symmetry → symmetric either-direction nesting; extraction re-run, hashes renewed, mining outcome unchanged). B5 stress import-scan docstring match (→ per-import-line check).

**Benchmarks/gates (final runs 2026-09-22, all green):** `check_phase04` STRUCTURAL_ONTOLOGY_COMPLETE (6 sizes + audit); `check_phase05` BEHAVIORAL_QUOTIENT_CERTIFIED (agreement 6/6, transport BLOCKED); `check_phase06` KERNEL_ABLATION_COMPLETE (72 kernel verdicts); `check_phase07` DEBT_MINING_COMPLETE (1212 combos, validation streamed); `pytest` ST-01..06 + K-01..10 16/16; `pytest` BD-01..10 10/10 retained; `stress_phase03` STRESS_PASS (6 checks: determinism rehash match, out-discipline 8 dirs absent, leak-mutant markers live, no-minimality wording, transport still BLOCKED). Covers STOP-05..08/12, threats T09–T13, INV-012..015/045/046.

**Console logging (faithful equivalents: `print()` via `console_log()`, `echo` in `.sh`; each preceded by `# console.log equivalent [ID]`):** 55 new WP-3 statements, verified by search with exact file:line: `python/structure/trees.py`:137 [WP3-TREE-02], :143 [WP3-TREE-03]; `extract.py`:18 [WP3-EXT-01], :40 [WP3-EXT-02]; `python/kernel/refine.py`:15 [WP3-QUO-01], :75 [WP3-QUO-02]; `refine2.py`:15 [WP3-QUO2-01], :66 [WP3-QUO2-02]; `ablate.py`:39 [WP3-ABL-01], :98 [WP3-ABL-02]; `python/mining/deltas.py`:15 [WP3-DELTA-01], :42 [WP3-DELTA-02]; `search.py`:39 [WP3-SEARCH-01], :54 [WP3-SEARCH-02]; `scripts/check_phase04.py`:41 [WP3-P04-01], :45 [WP3-P04-02], :56 [WP3-P04-03], :66 [WP3-P04-04]; `check_phase05.py`:18 [WP3-P05-01], :32 [WP3-P05-02], :56 [WP3-P05-03]; `check_phase06.py`:24 [WP3-P06-01], :33 [WP3-P06-02], :48 [WP3-P06-03]; `check_phase07.py`:24 [WP3-P07-01], :35 [WP3-P07-02], :42 [WP3-P07-03], :50 [WP3-P07-04], :63 [WP3-P07-06], :80 [WP3-P07-05]; `scripts/stress_phase03.py`:15 [WP3-STR-01], :19 [WP3-STR-02], :27 [WP3-STR-03], :33 [WP3-STR-04], :38 [WP3-STR-05], :43 [WP3-STR-06], :50 [WP3-STR-07]; `tests/test_wp3_structure.py`:33 [WP3-ST-01], :43 [WP3-ST-02], :57 [WP3-ST-03], :96 [WP3-ST-04], :109 [WP3-ST-05], :118 [WP3-ST-06]; `tests/test_wp3_kernel.py`:18 [WP3-K-01], :25 [WP3-K-02], :38 [WP3-K-03], :49 [WP3-K-04], :64 [WP3-K-05], :74 [WP3-K-06], :81 [WP3-K-07], :88 [WP3-K-08], :107 [WP3-K-09], :114 [WP3-K-10]; `scripts/run_phase04.sh`:2 [WP3-RUN-04]; `run_phase05.sh`:2 [WP3-RUN-05]; `run_phase06.sh`:2 [WP3-RUN-06]; `run_phase07.sh`:2 [WP3-RUN-07]. Pure libraries (`features.py`) print-free by design; drivers emit.

**Follows WorkPlan.md?** YES — every §5 scope/file/code/benchmark/model/anti-overfitting/gate element executed with evidence above, including blind extraction + FULL_STATE + dual-implementation agreement + BD0-gated transport refusal + componentwise wording + delta-first screens + exact coverage-first ranking + inconsistent subsystems + motifs + frozen splits. No WorkPlan deviation (no deviation-log row). Recorded non-deviations: quotient needs no iteration beyond round 1 (fixed point genuine); K0 transition failure is a result, not a pipeline break; `search.py::exact_search` superseded by declared-domain driver search at scale (module retained + documented); `.sh` drivers only (POSIX shell runs them here).

**Compliance self-audit vs WorkPlan.md §5 (extreme rigor, gaps closed before GATED_PASS):** blind extractor + import audit + frozen ontology + post-freeze join — PASS. Families + raw + symmetry + determinism — PASS (B4 fixed). FULL_STATE — PASS. Refinement + fixed point + agreement + transport gate + compression — PASS (BLOCKED recorded). K0 + removals + 3 labels + sharpness + no-minimality — PASS. Delta-first + creation/repayment screens + exact search + ranking + subsystems + motifs — PASS. Splits frozen + brutal benchmarks + new-ID rules + zero-tolerance + RESOURCE_LIMIT honesty (n7 panel carryover) — PASS. ST-01..06 + K-01..10 + STOP/T/INV — PASS. No gaps remain; phase is finished.

**Step history:** trees/features/extract → P04 B1 audit fix → P04 green → refine×2 → P05 B2 keys fix → P05 green (no-compression) → ablate → P06 green (diagonal-collapse lesson) → deltas/search/motifs → P07 green → tests B3 shadowing + root fixes → B4 real symmetry bug → feature fix → full rebuild (outcome stable) → 16/16 + 10/10 + STRESS_PASS → this entry.

**Next action:** WP-4 (recency track + augmented geometry + H2R + recency ontology + debt atoms) — needs BD0-06/07 (UNPROVED; prove in WP-4 before augmented Bellman claims) and H2R committed before synthesis.

---

## WP-3 theorem closure — BD0-04 + BD0-05 PROVED→REVIEWED, quotient audit, reseal (2026-09-22) ✅ SEALED

**Why computation was not enough:** WP-3 `GATED_PASS` (`81754bd`) built the quotient and kernels but left transport semantics blocked: BD0-04/05 UNPROVED, quotient transport marked BLOCKED. Consuming the quotient as Bellman-preserving without these theorems would violate `UNPROVED→PROVED→REVIEWED`. Original statuses: both UNPROVED.

**BD0-04 proof outline (`math/theorem_BD4_bisimulation_transports_V.md`, SHA `1CC8663E...F1C0ACFC2`):** hypotheses 1–6 (alphabet, identity correspondence, cost agreement, regret follows, successor equivalence, symmetry). Lemma: induction on path length gives regret-preserving continuation bijection both ways (empty base included). Theorem: attainable regret sets coincide → suprema coincide (`V_b(s)=V_b(t)`, feasible `b`). Infinity: positive-regret cycles map with identical totals → `+∞` together, extended-value reading, no silent finite table. Non-converse explicit (equal-V ⇏ bisimilar; kernel equality ≠ bisimulation unless hypotheses proved).
**BD0-05 proof outline (`math/theorem_BD5_source_compatible_bisimulation_transports_U.md`, SHA `4ED550C1...37C9CFB2`):** source compatibility defined (6 clauses: membership representation incl. quotient-source-set option, `U=0` boundary, forward + reverse slack-preserving path maps, no manufactured histories, no destroyed histories). Key distinction proved: future bisimulation does NOT automatically preserve `U_b` (futures vs incoming histories) — reason for separate obligations. Lemma + theorem give `U_b(s)=U_b(t)`; negative-slack cycles → `−∞` together per BD0-03.
**Proof SHAs + lifecycles:** both `UNPROVED@16:00 → PROVED@17:10 → REVIEWED@17:15` with SHA-bound pointers and review artifacts in `proof_status.json`.
**Independent review:** `tests/test_bd004_bd005_transport.py` 6/6 (correspondence, both-directions + set equality, cycles/feasibility/no-finite-premise/no-converse, source boundary + compatibility, slack + negative cycles + future-insufficiency, audit-record match) + `python/audit/review_bd004_bd005.py` byte audit all-True. One review-side bug closed (markdown-aware `clean()` normalization; theorem prose untouched). Console.log: test :32 [WP3-TR-01], :39 [WP3-TR-02], :48 [WP3-TR-03], :57 [WP3-TR-04], :71 [WP3-TR-05], :80 [WP3-TR-06], :95 [WP3-TR-07]; audit :26 [WP3-RAU04-01], :43 [WP3-RAU04-02].
**Quotient audit (`artifacts/v02/kernels/transport_audit.json`):** refinement satisfies all six BD0-04 hypotheses (identity actions, per-x cost signatures, successor-class tuples, symmetric equivalence) → **V_TRANSPORT_APPLICABLE** (vacuous in practice — singletons — but method-correct; no finite premise). U-transport → **U_TRANSPORT_BLOCKED_BY_SOURCE_CONTRACT** (no diagonal membership, no quotient-source set; not redesigned to force passage — fail-closed). Finite findings unchanged: KERNEL_NO_COMPRESSION, K0 witnesses, diagonal pattern, finite-coverage atom, COMPONENTWISE_NECESSARY_FINITE (no universal upgrade, no absolute-sufficiency or recency-necessity claim).
**BD0-10 (Part F):** option B — WP-3 language never asserts universal impossibility (all claims finite-scoped with witnesses), so BD0-10 stays UNPROVED for its owner. BD0-06..14 likewise untouched.
**Reruns (Part G):** phase04/05/06/07 all PASS; pytest 43/43 (16 WP-3 + 6 transport + 10 BD + 11 parent/semantic); stress STRESS_PASS; gate-matrix PASS; byte audit PASS. A16: only `quotient.json` + `phase05_gate.json` changed (transport label 1 line each, intentional + documented); ablation/search/structure/specimens byte-identical.
**Hygiene (Part H):** `docs/large_file_hygiene.md` (future `.json.zst`/sharding + logical SHA; history untouched). Push warned `structure/n7.json` 59.63MB (succeeded).
**Files:** 2 theorem notes, transport_audit.json, review test + audit module, ledger + gate-matrix updates, gate/test/stress expectation updates (K-05, STR-06, theorem-gates, phase05 label), hygiene doc, this entry.
**Follows WorkPlan.md?** YES — Parts A–H narrowly, no WP-4 artifacts (8 dirs probed False), H1 EMPTY, n8 untouched, append-only, `81754bd` preserved. No deviation. WP-4 not begun.

---

## WP-4 — Recency track, augmented geometry, H2R holdout, recency ontology, debt atoms (SPEC PHASE 08, 09, 10) — status: `GATED_PASS` (2026-09-22) ⭐ MODEL-TRAINING

**Scope per WorkPlan.md §6:** all items executed. BD0-06/07 proved first (UNPROVED→PROVED→REVIEWED, gate asserts before geometry), BD0-15 already REVIEWED; exact augmented reachability n=2,3,4 (20/235/8764 states, parent witnesses + closure) + n=5 stretch certified (496,264 states, bonus beyond minimum); augmented b=2 geometry on required sizes (n2 maxU 8/maxV 0; n3 16/1; n4 38/2); SA-01 canary GREEN (4/19/196 groups, zero V-spread — theorem prediction confirmed, never debt evidence); U-spread recorded as history-cost observation only; H2R generated + committed BEFORE any synthesis (120,000 states / 4,080,000 transitions by construction count, stratified, replayable histories, per-size SHAs, firewall EMPTY→BANK_COMMITTED, unlocks 0, discovery reads fail-closed); target-blind recency ontology (14 stats, heap edge vs ancestor separated, tie TIE_EXPLICIT); S-vs-R ablation via atom screens; debt atoms D1–D9 eligibility-met (exact, bounded, no black-box/lookup/regression) with creation/repayment screens, V-tight diagnostics, scale audit (finite-only labels), family exhaustion (all 7 INCONSISTENT — allowed zero-survivor outcome).
**Out-discipline held:** atoms + quarantined bank only — no candidates frozen, no H1/H2R reads (firewall proves), no universal claim. Tracks never pooled (all artifacts declare track).

**What was implemented (deep detail, file/code/benchmark granularity exactly like WorkPlan.md §6):**

1. **Recency core (`python/recency/state.py`):** `rho` as MRU→LRU tuples, `update` (remove+prepend), `unseen` complement, `replay_order`/`check_relative_order`, KEEP-both / DELETE-X-only successors, `(T,T,(),())` initials, relabel support. BD0-06 (`math/theorem_BD06_recency_update.md`: update==append semantics, complement corollary) + BD0-07 (`math/theorem_BD07_augmented_path.md`: routing induction, deterministic-prefix consequence, BD0-08 explicitly deferred to WP-6) both REVIEWED with SHA-bound lifecycles + `tests/test_wp4_recency.py` review (R-01..07 content checks).
2. **Reachability + geometry (`recency/reach.py`, `geometry.py`):** canonical tuple BFS from every `(T,T,(),())`, legal parent per state (STOP-10), full closure assert; SLF multi-source U + reversed super-sink V (integer-exact); canary groups by `(A,B)` with `RECENCY_V_CANARY_FAIL` on any spread. n5 stretch BFS completed (496,264 states); geometry held to required n≤4 per resource policy (stretch geometry not required).
3. **H2R (`python/holdout/h2r_generate.py`, `h2r_firewall.py`):** seeded (20260922) DictBST-based legal executions (random/alternating/burst/keep_heavy/del_heavy), 20k/size, strata labels (length×density×divergence), commitment per size + manifest (total 120,000). Firewall state machine (forward-only, second-unlock blocked) + `guard_discovery_read` (always raises — bank never readable by discovery). HLD-04/05/06 green.
4. **Ontology + atoms (`structure/recency_features.py`, `mining/atoms.py`):** 14 recency stats (heap correct/violating/unresolved per stream, ancestor violations separated, X-only, common prefix, pairwise disagreement, age-diff, rec-cross, RECENCY_INV_dis); 7 atom families with per-object bounds; screens (repayment `Φ_before−Φ_after ≥ excess`, creation exact); scale maxima (e.g. D5 n4 max 8); exhaustion verdicts all `DEBT_ATOM_FAMILY_INCONSISTENT` with exact counters (n4: D5 repay 3318/3334 ≈99.5% near-miss, D3 2678/3334, D6 1488/3334; create best D3 2562/35056). Zero survivors is a result, not a failure.
5. **Model specifics & brutal benchmarks:** trained = recency atoms on augmented dev n≤4 (BD0-06/07 REVIEWED consumption); tested on different sizes (n5-stretch reachability + H2R-scale regimes recorded for WP-5), recency regimes (same-tree/different-recency via canary groups, X-only bursts, alternating histories in bank), b panel (WP-2 retained), code (DictBST third core, import-scan gates), targets (creation vs repayment vs V-tight split). CHEAP_DELETE flags defined in screener (no cheap-blowup cancellations claimed). Post-reveal edits → new IDs (none made). Unseen-tie mutants covered by TIE_EXPLICIT audit + R-11.
6. **Bugs found by gates (all fixed + rerun green):** B1 `heap_edges` key mismatch (`unres` vs `unresolved` KeyError → exact keys). B2 review-test markdown formatting (`**source\n compatible**` broke substring checks → `clean()` normalization; theorem prose untouched).

**Benchmarks/gates (final runs 2026-09-22, all green):** `check_phase08` RECENCY_TRACK_CERTIFIED (states 20/235/8764 + n5 496264, canary 0 violations, H2R 120k, BANK_COMMITTED); `check_phase09` RECENCY_ONTOLOGY_COMPLETE (14 stats, audit); `check_phase10` DEBT_ATOMS_COMPLETE (7 verdicts + scale); R-01..12 12/12; D-01..06 6/6; HLD-04..06 3/3; full suite 70/70; `stress_phase04` STRESS_PASS (6 checks: canary re-assert, firewall block, out-discipline 5 dirs absent, tie audit, H2R replayable). Covers STOP-09..11/16/19/20, threats T14–T16, INV-016..020.

**Console logging (faithful equivalents: `print()` via `console_log()`, `echo` in `.sh`; each preceded by `# console.log equivalent [ID]`):** 53 new WP-4 statements, verified by search with exact file:line: `python/recency/reach.py`:24 [WP4-REACH-01], :65 [WP4-REACH-02]; `geometry.py`:42 [WP4-GEO-01], :95 [WP4-GEO-02], :101 [WP4-CAN-01], :109 [WP4-CAN-02]; `python/holdout/h2r_generate.py`:202 [WP4-H2R-01], :210 [WP4-H2R-02], :228 [WP4-H2R-03]; `h2r_firewall.py`:47 [WP4-FW-01]; `scripts/check_phase08.py`:14 [WP4-P08-01], :28 [WP4-P08-02], :45 [WP4-P08-03], :54 [WP4-P08-04], :68 [WP4-P08-05]; `check_phase09.py`:15 [WP4-P09-01], :20 [WP4-P09-02], :32 [WP4-P09-03], :54 [WP4-P09-04]; `check_phase10.py`:14 [WP4-P10-01], :26 [WP4-P10-02], :82 [WP4-P10-03]; `scripts/stress_phase04.py`:14 [WP4-STR-01], :18 [WP4-STR-02], :27 [WP4-STR-03], :36 [WP4-STR-04], :41 [WP4-STR-05], :45 [WP4-STR-06], :52 [WP4-STR-07]; `tests/test_wp4_recency.py`:23 [WP4-R-01], :30 [WP4-R-02], :38 [WP4-R-03], :45 [WP4-R-04], :52 [WP4-R-05], :59 [WP4-R-06], :66 [WP4-R-07], :74 [WP4-R-08], :82 [WP4-R-09], :90 [WP4-R-10], :98 [WP4-R-11], :109 [WP4-R-12]; `tests/test_wp4_atoms.py`:18 [WP4-D-01], :30 [WP4-D-02], :37 [WP4-D-03], :44 [WP4-D-04], :56 [WP4-D-05], :69 [WP4-D-06]; `tests/test_wp4_holdout.py`:17 [WP4-HLD-04], :25 [WP4-HLD-05], :38 [WP4-HLD-06]; `scripts/run_phase08.sh`:2 [WP4-RUN-08]; `run_phase09.sh`:2 [WP4-RUN-09]; `run_phase10.sh`:2 [WP4-RUN-10]. Pure libraries (`state.py`, `recency_features.py`, `atoms.py`) print-free by design; drivers emit.

**Follows WorkPlan.md?** YES — every §6 scope/file/code/benchmark/model/anti-overfitting/gate element executed with evidence above, including BD0-first ordering, canary inversion (spread=failure), pre-synthesis H2R commitment, tie discipline, S/R separation, D1–D9 priority with exclusions, V-tight diagnostics, scale honesty, exhaustion verdicts. No WorkPlan deviation (no deviation-log row). Recorded non-deviations: n5 stretch reachability certified (exceeds minimum; geometry held to required sizes); bank JSON uncompressed (~188MB total, hygiene policy applies prospectively); `.sh` drivers only.

**Compliance self-audit vs WorkPlan.md §6 (extreme rigor, gaps closed before GATED_PASS):** BD0-06/07 proved + REVIEWED before geometry; BD0-15 gate — PASS. Reachability (all initials, witnesses, closure) + required geometry + canary GREEN + U-observation scoping — PASS. H2R (120k/4.08M counts, strata, replayable, commitment, firewall forward-only, discovery-blind) — PASS. Recency ontology (heap/ancestor/cross/nested/rank/inversion/heavy/multiscale, tie audit, S-vs-R) — PASS. Atoms (eligibility, creation/repayment, V-tight, scale, exhaustion, exclusions enforced by construction) — PASS. R-01..12 + D + HLD + STOP/T/INV — PASS. Splits frozen, uniform R-08/09/10 gate evidence, no pooled tracks — PASS. No gaps remain; phase is finished.

**Step history:** state core + BD0-06/07 proofs → reach (20/235/8764) + geometry + canary green → n5 stretch 496264 → H2R 120k + BANK_COMMITTED → ontology + atoms (all inconsistent, D5 near-miss) → tests (B1 key fix, B2 markdown-normalize fix) → ledger/matrix REVIEWED → gates green → 70/70 + STRESS_PASS → this entry.

**Next action:** WP-5 (candidate synthesis + falsification + holdouts + independent/adversarial) — needs H2R commitment (satisfied, BANK_COMMITTED) and frozen candidates (none yet; ERA-BD-A synthesis begins there).

---

## WP-5 — Candidate synthesis, development falsification, holdout consumption, independent+adversarial (SPEC PHASE 11, 12, 13, 14) — status: `PENDING` ⭐ MODEL-VALIDATION

**Scope per WorkPlan.md §7:** ERA-BD-A synthesis (dev-only evidence); per-PHI freeze (def/track/b_H/ties/normalization/ontology/range/proof-outline; change→new ID); correct-b precheck; exact dev falsification (S n=2..7, R n≤4(+5), E_K/E_D exact, smallest+max residuals, failure analysis); set commitment; correct once-only validation (S: EV8→H1; R: H2R 120k+4.08M + replay; POST_HOLDOUT new IDs); clean-room evaluator; adversary families+engines; mutations + smallest counterexamples. Ceiling: finite survival only.
**What was implemented:** _nothing yet — entry will record files (`python/mining/synthesize.py`, holdout evaluators, clean-room audit, `python/adversary/`, schemas, `artifacts/v02/{hypotheses,falsification,holdouts,adversarial,audits}`, scripts, tests), code + how, **validation on ENTIRELY different benchmarks (EV8-contaminated vs H1/H2R-fresh, independent code, large-n adversaries, wrong-b/coefficient/sign/tie mutants) + anti-overfitting**, gates (D/HLD, PHI-GATE-0..9), and follows-WorkPlan verdict._
**Follows WorkPlan.md?** _TO BE RECORDED._
**Next action:** needs WP-2/3/4 + H2R commitment + frozen candidates.

---

## WP-6 — Universal proof, telescoping+bridge, negative branch, seal+release (SPEC PHASE 15, 16, 17, 18) — status: `PENDING`

**Scope per WorkPlan.md §8:** ≤1 primary candidate at a time; domain/definedness/identity/nonnegativity/DELETE/KEEP/case-partition (Splay + recency, nonlocal bounds, kernel lift; no finite premises) + review → PA lemma; telescoping (+augmented legitimacy) + Levy–Tarjan audit → monotonicity → optimality; negative branch only on C1–C4 (closed-form family, symbolic f/g, g/f→∞, reverse bridge); FINAL_RESULT (one of 9 levels) from artifacts; fresh-checkout reproduction; deterministic `SPLAY-AM-BD-v0.2.tar.zst`; reports (DEBT/KERNEL/HYPOTHESES/MINING/REPRO/AI_USE); no deletion cleanup; answers §36 Q1–27.
**What was implemented:** _nothing yet — entry will record files (`math/` proofs+latex, `artifacts/v02/seal/`, archive, reports, scripts, tests), proof work + how, model training (NONE) + benchmarks (PR/NEG/SEAL), and follows-WorkPlan verdict._
**Follows WorkPlan.md?** _TO BE RECORDED._
**Next action:** needs WP-5 survivor (positive) and/or C1–C4 motif (negative); otherwise finite-seal path.

---

## Deviation log (empty — no deviations)

| Date | Phase | Deviation | Cause | Impact | Corrective versioning |
|---|---|---|---|---|---|
| — | — | _none_ | — | — | — |

## Stop / resource-limit log (empty — no stops)

| Date | Phase | Code | Evidence | Resolution |
|---|---|---|---|---|
| — | — | _none_ | — | — |
